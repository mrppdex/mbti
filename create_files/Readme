## Create Files

Run `make.sh` to create feature transformation objects as well as train and save the model file. Once the files are created and copied, you can create Docker containers in the `model_container` directory.

All `mbti.*` files represent different project stages.